body {
    font-family: Arial, sans-serif;
    background-image: url('barberia1.jpg'); /* Asegúrate de que el nombre del archivo sea exacto */
    background-size: cover;
    background-position: center;
    margin: 0;
    padding: 0;
    text-align: center;
    color: #fff; /* Cambia el color del texto a blanco para mejor contraste */
}

header {
    background-color: rgba(51, 51, 51, 0.8); /* Fondo semitransparente */
    color: #fff;
    padding: 1em 0;
}

.logo {
    width: 100px; /* Ajusta el tamaño del logo según sea necesario */
    height: auto; /* Mantiene la proporción del logo */
    display: block;
    margin: 0 auto 10px; /* Centra el logo horizontalmente y añade un margen inferior */
}

h1 {
    margin: 0;
}

main {
    padding: 2em;
    background-color: rgba(0, 0, 0, 0.5); /* Fondo semitransparente para mejorar la legibilidad */
    border-radius: 10px;
    display: inline-block;
}

#horarios ul {
    list-style: none;
    padding: 0;
}

#horarios li {
    background-color: #444; /* Cambia el color de fondo del botón */
    color: #fff; /* Cambia el color del texto del botón */
    margin: 0.5em 0;
    padding: 0.5em;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s ease, transform 0.3s ease; /* Añade transición para suavidad */
}

#horarios li:hover {
    background-color: #666; /* Color de fondo al pasar el ratón por encima */
    transform: scale(1.05); /* Aumenta ligeramente el tamaño al pasar el ratón */
}

#contacto p {
    margin: 0.5em 0;
}
.slogan {
    font-size: 1.2em; /* Tamaño de fuente ajustado */
    color: #e6e6e6; /* Color de texto gris claro */
    margin-top: 10px;
    font-style: italic; /* Estilo en cursiva para destacar */
}
.slogan {
    font-size: 1.2em; /* Tamaño de fuente ajustado */
    color: #e6e6e6; /* Color de texto gris claro */
    margin-top: 10px;
    font-style: italic; /* Estilo en cursiva para destacar */
}
